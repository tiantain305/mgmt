apiVersion: infra.nephio.org/v1alpha1
kind: Repository
metadata: # kpt-merge: default/example-cluster-name
  name: nephio-workload-cluster-repo
  namespace: default
  annotations:
    internal.kpt.dev/upstream-identifier: infra.nephio.org|Repository|default|example-cluster-name
spec:
  description: nephio-workload-cluster-repo repository
  defaultBranch: main
