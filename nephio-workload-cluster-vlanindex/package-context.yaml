apiVersion: v1
kind: ConfigMap
metadata: # kpt-merge: /kptfile.kpt.dev
  name: kptfile.kpt.dev
  annotations:
    config.kubernetes.io/local-config: "true"
    internal.kpt.dev/upstream-identifier: '|ConfigMap|default|kptfile.kpt.dev'
    nephio.org/cluster-name: nephio-workload-cluster
data:
  name: nephio-workload-cluster-vlanindex
  package-path: nephio-workload-cluster-vlanindex
